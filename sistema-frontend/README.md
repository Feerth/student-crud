# Frontend - Sistema de Gestión de Estudiantes

Interfaz de usuario web para el sistema de gestión de estudiantes, desarrollada con React.

## 📋 Requisitos Previos

- **Node.js** (Versión LTS recomendada)
- **npm** (Gestor de paquetes incluido con Node.js)

## ⚙️ Instalación

1.  Navega al directorio del frontend:
    ```bash
    cd sistema-frontend
    ```
2.  Instala las dependencias del proyecto:
    ```bash
    npm install
    ```

## 🚀 Ejecución en Desarrollo

Para iniciar la aplicación en modo de desarrollo:

```bash
npm start
```

- La aplicación se abrirá automáticamente en `http://localhost:3000`.
- Asegúrate de que el **Backend** esté en ejecución para que la aplicación funcione correctamente.

## 📦 Construcción para Producción

Para generar una versión optimizada para producción:

```bash
npm run build
```

Esto creará una carpeta `build` con los archivos estáticos listos para desplegar.
